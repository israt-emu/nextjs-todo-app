/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.jsdelivr.net", "res.cloudinary.com"], // Add any other domains you need here
  },
};

export default nextConfig;
